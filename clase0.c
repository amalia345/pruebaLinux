//bubble sort
// ultima i -1
//nummero de aargumentos que quiere ordenar 
#include<stdio.h>

int main([])
{
    //ciclo de j 
for (i=n-1; i>j; i-- );//el ciclo de i lo ejecuto n+1 veces 
    if(a[i]<=a[i-1]){
        t=a[i];
        a[i]= a[i-1]
        a[i-1]=t;
    }

    /* code */
    return 0;
}